<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.g6.cricspot">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MatchActivity"></activity>
        <activity android:name=".TeamDetailsForMatchActivity" />
        <activity android:name=".FindMatchActivity" />
        <activity android:name=".PlayerDetailsActivity" />
        <activity android:name=".FindPlayerActivity" />
        <activity android:name=".UserWithTeamActivity" />
        <activity android:name=".ConfirmCreatedTeamActivity" />
        <activity android:name=".CreateTeamActivity" />
        <activity android:name=".TeamDetailsActivity" />
        <activity android:name=".UserWithoutTeamActivity" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>